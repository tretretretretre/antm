# Copy to compose/.env and replace every placeholder locally.
# Never commit compose/.env.

AINTM_TG_CHAT_ID=replace-with-approved-chat-id
POSTIZ_JWT_SECRET=generate-a-long-random-secret
POSTIZ_DATABASE_PASSWORD=generate-a-unique-database-password
TEMPORAL_DATABASE_PASSWORD=generate-a-different-database-password
