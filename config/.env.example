# Copy to config/.env and fill only the services you intentionally enable.
# config/.env is ignored by Git.

GEMINI_API_KEY=replace-if-google-generation-is-enabled
POSTIZ_API_KEY=replace-only-after-publishing-is-approved
POSTIZ_URL=http://127.0.0.1:5000
AINTM_IMAGE_MODEL=gemini-3.1-flash-image
